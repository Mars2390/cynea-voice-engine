[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cynea-voice-engine"
version = "0.1.0"
description = "Cynea AI Voice Agent Platform"
requires-python = ">=3.10"
